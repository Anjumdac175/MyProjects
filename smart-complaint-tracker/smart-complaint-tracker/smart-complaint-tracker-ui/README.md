# Smart Complaint Tracker UI

React + Vite + Bootstrap frontend for Smart Complaint Tracker API.

## Run
```bash
npm i
npm run dev
# open http://localhost:5173
```
Make sure the backend is running at `http://localhost:8080`.
